import React from "react";
import { Typography } from "@material-ui/core";

const About = () => {
  return (
    <div>
      <Typography variant="h2">About page</Typography>
      <Typography variant="body1">Helpers data</Typography>
    </div>
  );
};

export default About;
